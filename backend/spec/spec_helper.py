# This file is loaded before any tests
# Django should be loaded at this point and models are available if you are using `python manage.py mamba`


def configure(mamba_settings):
    # Do nothing
    return
